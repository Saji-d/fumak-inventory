package com.fumak.scanner.ui.nav

/** Plain string routes — just the scanner and its connection settings. */
object Routes {
    const val SCANNER = "scanner"
    const val CONNECTIONS = "connections"
}
